# TwoSum-LeetCodes
This C++ program solves the classic Two Sum problem using an object-oriented approach. It stores user input in an array and uses nested loops to check all possible pairs to find two numbers that add up to a given target. If a matching pair is found, their indices are printed; otherwise, it displays that no solution exists.
